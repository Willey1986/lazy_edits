import { GetAmountPipe } from './get-amount.pipe';

describe('GetAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
